# CONTRIBUTING

本文是量潮知识工作规格的撰写与修改方法，供贡献者参考。

规格是契约，不是教程或描述：它约束实现，同时为实现保留自由。写作的首要纪律是克制——写下的每个字都是条款，少写不是没写完，多写可能是越权。

## 判层先行

本系列分两层：各组的 `index.md` 是概括层，回答领域模型、事件与 API 的概貌；实体篇是明细层，回答模型规格明细、事件规格明细、API 规格明细。动笔前先判所改篇目属于哪一层，两层写法相反，判错层越改越偏。

### 概括层

概括层回答「是什么」，为明细层留出空间：

- 先审概念性质，再选结构：概念间关系是什么性质（层级容器、抽象与具体、输入与输出等）就写成什么关系，不得因结构相似而套用；
- 事件保持开放：用概括表述描述事件（与 X 相关的生命周期事件，如……），不写成封闭集合——枚举即约束；
- 细节下沉：实现细节、参数、完整枚举不进概括层，靠引用明细衔接；
- 不解释为什么：只规定是什么，不论证设计动机；
- 各篇对齐：术语、句式、板块结构与同层已定稿篇目一致，同层定稿即该层模板。

### 明细层

明细层是概括层承诺的具体化，写法与概括层相反：封闭而穷尽，该明细范围内的条目尽可能列举完整；精确无歧义，每条规格须尽可能可被实现方直接执行。

## 精度取最小契约

范围取各平台必须一致的最小集，不求穷尽。事件名、字段名与类型、端点的方法与路径要定死；请求体细节、状态码、未规定的操作不写，留给平台权衡。判据是「实现方据此能否判断做法合不合规」，能判即够。

## 单一事实源

每条事实只写一处，其余靠引用。层与层的对应不算重复，抢了别的概念的明细才算，例如在 `workspace.md` 里列举材料、产物的事件。

## 通用规范

- 不带编辑性附注：不写「建议」「待确认」等对话式内容；
- 修改默认收敛：以删减、概括为主，不补充、解释；
- 每次修改只解决一个问题。

## 共用版面

同层篇目用同一套子块：`## 领域模型` 分字段、关联、约束，`## 领域事件` 分事件、约束，`## API 端点` 分资源端点、子资源端点、约束。

字段清单每行同一模板：

```markdown
- `字段`（类型，必选性）：说明。
```

必选性取必选、推荐、只读之一。

## 口径一致

约束一律称「约束」，不为每节另造词。中文标点全角，句末用句号；列举用「或」，不用「／」；同一术语全文同名。
