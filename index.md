# 首页

量潮知识工作规格是量潮知识工作标准化体系的核心规范之一，与案例库等配套材料共同构成该领域的标准化依据。

本文档属**规定性规范**：定义了知识工作领域的通用语言、领域模型、领域事件与 API 规格，明确各平台”必须“遵循的概念与接口；案例等其他材料属**示范性内容**，说明规范在实际场景中”如何“应用，并承载实践对规格的反馈。

量潮工作云及其他量潮工作平台在设计、实现与维护时，原则上以本文档为准。实践中经检验有效的偏离，可通过案例沉淀并作为修订本文档的依据。

## 领域模型

本领域的核心领域模型可概括为 3P 模型——因其英文名称 Piece、Process、Place 的首字母均为 P 而得名，对应中文为工件、过程、场所，分为三组：

- **工件组（Piece）**：描述知识工作中被加工与产出的对象，包括材料（Material）与产物（Artifact）；
- **过程组（Process）**：描述对材料的处理过程，包括工单（WorkOrder）与工作流（Workflow）；
- **场所组（Place）**：描述工作发生的场所，包括工作区（Workspace）与工作台（Workbench）。

## 领域事件

领域事件是账本的投递出口——是通知，不是正本：负载只指认事实（事件名、时刻与相关的 `id`），正文的住所是账本，下游要正文凭 `id` 回读；同一 `id` 的重放视作重放，幂等消化。

本领域的主要领域事件包括：材料已收录（Material Imported）、工作流已创建（Workflow Created）、工单已创建（WorkOrder Created）、工作记录已追加（WorkRecorded）、产物已生成（Artifact Produced）、产物已验收（Artifact Accepted）等。

以下典型场景可串联上述事件与领域模型：用户将一组材料（如飞书消息、飞书文档）收录至属于自己的工作区（触发材料已收录）；在该工作区内设计一条分步骤的工作流，如按时间排序信息、梳理主线、撰写成文（触发工作流已创建）；随后开工单逐站推进，每过一站记一条工作记录（触发工单已创建、工作记录已追加）；走完后经用户验收，最终得到预期的产物，如一份工作手册（触发产物已生成、产物已验收）。

## API 规格

本领域的 API 设计同时遵循 REST 架构风格与领域驱动设计原则：领域实体与值对象建模为 REST 资源，通过标准 HTTP 方法暴露端点；领域服务实现为无状态服务，不维护会话状态。端点 URL 全用名：名链自带辖区，从工作台到工单逐段限定；`id` 是身份不是地址——随事件负载走，供跨区归并与幂等去重。各资源与端点的详细定义见 API 规格明细。

